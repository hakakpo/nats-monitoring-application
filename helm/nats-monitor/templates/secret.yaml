{{- if .Values.smtp.username }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nats-monitor.fullname" . }}-smtp
  labels:
    {{- include "nats-monitor.labels" . | nindent 4 }}
type: Opaque
data:
  username: {{ .Values.smtp.username | b64enc | quote }}
  password: {{ .Values.smtp.password | b64enc | quote }}
{{- end }}
